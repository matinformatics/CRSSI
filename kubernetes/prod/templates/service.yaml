apiVersion: v1
kind: Service
metadata:
  name: {{ include "crssi.fullname" . }}
  labels:
    {{- include "crssi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crssi.selectorLabels" . | nindent 4 }}
